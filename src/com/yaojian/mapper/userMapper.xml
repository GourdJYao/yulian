<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace：必须与对应的接口全类名一致 id:必须与对应接口的某个对应的方法名一致 -->
<mapper namespace="com.yaojian.mapper.UserMapper">
    
    <resultMap id="resourceResultMap" type="com.yaojian.model.User">
		<result column="id" property="id" />
		<result column="username" property="username" />
		<result column="password" property="password" />
		<result column="email" property="email" />
		<result column="updatedate" property="updatedate" />
	</resultMap>

	<insert id="save" parameterType="User">
		insert into
		t_user_info(username,password,email,updatedate)
		values(#{username},#{password},#{email},#{updatedate});
	</insert>

	<update id="update" parameterType="User">
		update t_user_info set
		id=#{id},username=#{username},password=#{password},email=#{email},updatedate=#{updatedate}
		where id=#{id}
	</update>

	<delete id="delete" parameterType="int">
		delete from t_user_info where
		id=#{id}
	</delete>

	<!-- mybsits_config中配置的alias类别名,也可直接配置resultType为类路劲 -->
	<select id="findById" parameterType="int" resultType="User">
		select
		id,username,password,email,updatedate
		from t_user_info where
		id=#{id};
	</select>

	<select id="findAll" resultType="User">
		select
		id,username,password,email,updatedate
		from t_user_info;
	</select>
	
	<select id="findByUser" parameterType="User" resultType="User">
		select
		id,username,password,email,updatedate
		from t_user_info WHERE username=#{username} AND password=#{password};
	</select>
</mapper>